# Usage

Copy the `flake.nix` file for a Python project into your project by executing

```bash
nix flake init -t github:erwinminties/nix-templates#python
```

This flake.nix comes with:
- pip
- uv
- ruff
- pre-commit
